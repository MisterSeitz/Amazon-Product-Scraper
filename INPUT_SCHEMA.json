{
  "title": "Amazon Product Scraper Input Schema",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "search": {
      "title": "Search Query",
      "type": "string",
      "description": "The keyword or product name to search for on Amazon.",
      "editor": "textfield"
    },
    "maxItems": {
      "title": "Maximum Items",
      "type": "integer",
      "description": "The maximum number of products to scrape.",
      "default": 50
    },
    "mode": {
      "title": "Scraper Mode",
      "type": "string",
      "description": "Choose how the scraper runs.",
      "enum": [
        "search",
        "asin",
        "url"
      ],
      "enumDescriptions": [
        "Search mode: Provide a keyword to search products on Amazon.",
        "ASIN mode: Provide one or more ASINs to scrape specific products.",
        "URL mode: Provide a list of Amazon product URLs to scrape."
      ]
    },
    "asins": {
      "title": "ASINs",
      "type": "array",
      "description": "List of ASINs to scrape if using 'asin' mode.",
      "items": {
        "type": "string"
      }
    },
    "urls": {
      "title": "Product URLs",
      "type": "array",
      "description": "List of Amazon product URLs to scrape if using 'url' mode.",
      "items": {
        "type": "string",
        "format": "uri"
      }
    }
  },
  "required": ["mode"]
}
